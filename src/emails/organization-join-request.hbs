{{!-- layout variables --}}
{{!-- subject, preheader provided from server --}}
<h1 style="margin:0 0 8px 0;color:{{BRAND.text}};font-size:22px;">
  New Organization Join Request 🏢
</h1>

<p style="margin:0 0 14px 0;color:{{BRAND.muted}};">
  Hi{{#if name}} {{name}}{{/if}}, <strong>{{fromName}}</strong> has requested to join your organization on <strong>{{BRAND.name}}</strong>.
</p>

{{#if message}}
<div style="margin:16px 0;padding:12px;background:{{BRAND.bg}};border-radius:8px;color:{{BRAND.text}};">
  <p style="margin:0;font-style:italic;">"{{message}}"</p>
</div>
{{/if}}

{{> button href=reviewUrl label="Review Join Request"}}

<p style="color:{{BRAND.muted}};font-size:13px;margin:12px 0 0 0;">
  You can also review and respond to this request in your organization dashboard.
</p>

<hr style="border:none;border-top:1px solid {{BRAND.border}};margin:22px 0;" />

<p style="margin:0;color:{{BRAND.muted}};font-size:13px;">
  {{#if expiresInDays}}
  This request will expire in {{expiresInDays}} days if not reviewed.
  {{else}}
  This request will expire in 7 days if not reviewed.
  {{/if}}
</p>